<form method="post" action="{{ path('rapport_delete', {'id': rapport_activite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rapport_activite.id) }}">
    <button class="btn">Delete</button>
</form>
